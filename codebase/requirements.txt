# --- CP2 baseline (used now) ---
# stdlib only: csv, dataclasses, datetime, pathlib, argparse -- nothing to install.

# --- Discord delivery (--send-to-discord), used now ---
python-dotenv>=1.2,<2.0

# --- Interactive demo bot (bot_gateway.py), used now --
# Only the Gateway/slash-command layer needs this; data/discord_live.py's
# REST fetch stays plain urllib, unchanged.
discord.py>=2.4,<3.0

# --- CP3: LangGraph decision and evaluation pipelines ---
langgraph>=0.2,<0.3
langchain-core>=0.3,<0.4
langchain-openai>=0.2,<0.4
langchain-google-genai>=2.0,<3.0
google-genai
